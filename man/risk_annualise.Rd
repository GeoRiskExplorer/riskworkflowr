% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_annualise.R
\name{risk_annualise}
\alias{risk_annualise}
\title{Annualise observed event counts}
\usage{
risk_annualise(
  data,
  count_col = "event_count",
  period_col = NULL,
  period_value = NULL,
  start_col = NULL,
  end_col = NULL,
  start_date = NULL,
  end_date = NULL,
  annualised_col = "annualised_count",
  period_years_col = NULL,
  year_length = 365.2425
)
}
\arguments{
\item{data}{A data frame, tibble, or sf object.}

\item{count_col}{Name of the observed event count column.}

\item{period_col}{Optional numeric column containing observation periods in
years.}

\item{period_value}{Optional fixed observation period in years.}

\item{start_col}{Optional column containing observation start dates.}

\item{end_col}{Optional column containing observation end dates.}

\item{start_date}{Optional fixed observation start date.}

\item{end_date}{Optional fixed observation end date.}

\item{annualised_col}{Name of the output annualised count column.}

\item{period_years_col}{Optional output column containing the calculated
observation period in years.}

\item{year_length}{Number of days used to represent one year. Defaults to
365.2425.}
}
\value{
The input object with an annualised count column and, optionally,
the calculated observation period in years.
}
\description{
Standardises observed event counts to an annual expected frequency using an
explicit observation period.
}
\details{
The observation period can be supplied as:
\itemize{
\item a fixed number of years using \code{period_value};
\item a numeric column of years using \code{period_col};
\item a fixed start and end date; or
\item row-specific start and end date columns.
}

Date-based periods are calculated as inclusive elapsed calendar days divided
by \code{year_length}. The default year length is 365.2425 days.

The function performs no grouping or aggregation. It is intended to operate
on already-prepared analytical units and can be piped into downstream risk
functions such as Poisson probability calculations.
}
\examples{
x <- data.frame(
  event_count = c(0, 5, 10)
)

risk_annualise(
  x,
  count_col = "event_count",
  period_value = 5
)

risk_annualise(
  x,
  count_col = "event_count",
  start_date = as.Date("2021-07-01"),
  end_date = as.Date("2026-06-30")
)

}
