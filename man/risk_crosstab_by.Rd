% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_crosstab_by.R
\name{risk_crosstab_by}
\alias{risk_crosstab_by}
\title{Build grouped categorical risk cross-tabs}
\usage{
risk_crosstab_by(
  data,
  by,
  rows,
  cols,
  value_col = NULL,
  measure = c("count", "sum"),
  start_date = NULL,
  end_date = NULL,
  expected_data = NULL,
  expected_col = "expected_count",
  poisson_k = 1,
  include_missing = TRUE,
  complete_rows = TRUE
)
}
\arguments{
\item{data}{A data frame or \code{sf} object.}

\item{by}{Character vector naming one or more grouping variables.}

\item{rows}{Single character string naming the row variable.}

\item{cols}{Single character string naming the column variable.}

\item{value_col}{Optional character string naming the value field used when
\code{measure = "sum"}.}

\item{measure}{Either \code{"count"} to count records or \code{"sum"} to sum
\code{value_col}.}

\item{start_date, end_date}{Optional analysis-period dates passed to
\code{\link[=risk_annualise]{risk_annualise()}}. Supply both or neither.}

\item{expected_data}{Optional data frame containing expected counts keyed by
\code{rows} and \code{cols}.}

\item{expected_col}{Character string naming the expected-count field in
\code{expected_data}.}

\item{poisson_k}{Whole-number Poisson exceedance threshold. Defaults to \code{1},
giving \code{P(X >= 1)}. For example, \code{poisson_k = 2} gives \code{P(X >= 2)}.}

\item{include_missing}{Logical. If \code{TRUE}, observed missing row/column
categories are retained as analytical \code{NA} values.}

\item{complete_rows}{Logical. If \code{TRUE}, unused factor levels in the row
variable are retained. If \code{FALSE}, only row groups observed in the input
are retained. Column factor levels are always retained.}
}
\value{
A non-spatial data frame containing grouping keys followed by the
standard \code{\link[=risk_crosstab]{risk_crosstab()}} analytical columns.
}
\description{
Repeats \code{\link[=risk_crosstab]{risk_crosstab()}} within one or more grouping variables. This is a
thin orchestration wrapper; all cross-tab calculations remain authoritative
in \code{\link[=risk_crosstab]{risk_crosstab()}}.
}
